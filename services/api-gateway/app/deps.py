"""Dependency injection utilities for the API gateway.

This module defines reusable dependency functions that can be
injected into route handlers via FastAPI's Depends mechanism. For the
mini project there are no dependencies defined yet, but the file
exists to demonstrate where one would put common providers such as
database connections, authentication handlers or configuration objects.
"""

# No dependencies at the moment