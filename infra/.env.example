## Environment configuration for the Intelligent Content Analyzer

# API provider configuration
MODEL_PROVIDER=GEMINI
OPENAI_API_KEY=
GEMINI_API_KEY=

# Embedding model to use
EMBEDDING_MODEL=bge-m3

# Vector store selection (FAISS, QDRANT, ELASTIC, PGVECTOR)
VECTOR_STORE=FAISS

# Reranker configuration (none, bge-reranker, cross-encoder-msmarco)
RERANKER=bge-reranker

# Language mode (multilingual, english-only, korean-only)
LANGUAGE_MODE=multilingual

# Caching configuration
CACHE_ENABLED=true
CACHE_REDIS_URL=redis://redis:6379/0

# Observability configuration
LANGFUSE_ENABLED=true
LANGFUSE_API_KEY=
LANGFUSE_HOST=http://langfuse:3000

# Confidence threshold for abstention (0.0–1.0)
CONFIDENCE_THRESHOLD=0.5

# Optional translation API key (for multilingual support)
TRANSLATION_API_KEY=

# Other secret keys can be added here